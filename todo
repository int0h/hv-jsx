+ arch
    + move HV into separate repo

- features
    + interpret HV in children as zone
    + nested zones
    + components!
    + wrap component content in a zone
    - events of components
    + use .attr or setAttribute depending on namespace
    + namespaces
    - consider not using both (.attr and setAttr for html namespace)
